{
  "rewrites": [
    {
      "source": "/mail-asih/(.*)",
      "destination": "/index.html"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "scripts": {
    "build": "npm install --legacy-peer-deps && next build"
  }
}
